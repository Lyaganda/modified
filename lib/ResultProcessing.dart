import 'package:flutter/material.dart';

class ResultProcessing extends StatelessWidget {

  final List<Map<int, String>> userAnswers;
  const ResultProcessing(this.userAnswers);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
       
      ],
    );
  }
}
